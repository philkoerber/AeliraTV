## world

Minimal multiplayer 3D web game stack.

### Quick start

From repo root:

```bash
docker compose -f world/docker-compose.yml up --build
```

- Web: `http://localhost:5173`
- Server: `http://localhost:2567`

